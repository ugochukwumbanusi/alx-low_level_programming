#include "main.h"
#include <stdio.h>
/**
 * mul - Multiple two integers
 * @a:The first integers
 * @b:The second integers
 * Return: Always 0
 */
int mul(int a, int b)
{
int result = a * b;
return (result);
}
